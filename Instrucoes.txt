python manage.py runserver 0.0.0.0:8000

python -m venv venv
venv\Scripts\activate

python -m pip install --upgrade pip
pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org

# 2. Executa as migrações e cria as tabelas de fato no banco
python manage.py migrate

python manage.py collectstatic

python manage.py createsuperuser

# 3. Importar máquinas do Excel (TAG DAS MÁQUINAS.xlsx)
python populate_machines.py

# Estrutura e Colunas Esperadas na Planilha Excel:
# - Nome do Arquivo: 'TAG DAS MÁQUINAS.xlsx' (deve estar na raiz do projeto)
# - Nome da Aba (Sheet): 'Cronograma 2026'
# - Linha de Cabeçalho (esperada até a linha 30, geralmente linha 11):
#   * A linha de cabeçalho deve conter as seguintes colunas (busca insensível a maiúsculas/minúsculas):
#     - 'TAG' (coluna com o identificador/tag do equipamento)
#     - 'Nome do Equipamento' (ou termo similar contendo 'nome' ou 'equipamento')
#     - 'Criticidade' (ou termo similar contendo 'criticidade', 'prioridade' ou 'crit')
#     - 'Setor' (ou termo similar contendo 'setor', 'área', 'linha' - caso ausente ou vazio, usa "Geral")
#
# Comportamento do Script:
# - Utiliza a biblioteca Pandas para ler e realizar a limpeza do Excel.
# - Remove linhas completamente vazias ou onde as colunas cruciais 'Setor' ou 'Nome do Equipamento' estão nulas.
# - Filtra linhas duplicadas do cabeçalho e rodapés da planilha.
# - Converte explicitamente os valores lidos para string e aplica `.strip()`, mantendo o formato original
#   e evitando que tags ou nomes puramente numéricos (como "102") sejam convertidos para float (como "102.0").
# - Cria/busca o Setor utilizando Sector.objects.get_or_create.
# - Cria/busca a Máquina utilizando Machine.objects.get_or_create com base tanto no Nome quanto no Setor.
# - Se a máquina já existir, atualiza sua criticidade caso tenha sido alterada na planilha.
# - Em caso de falha de importação em alguma linha específica, o script trata o erro e continua para as demais linhas.

# Notas do Sistema:
# - A senha padrão do superusuário admin é @Freedom1234
# - O Painel TV (Modo TV) foi otimizado para exibir mais de 20 técnicos sem rolagem (scroll)
# - Cores dinâmicas nos cards dos técnicos ativos/pausados são baseadas na criticidade da máquina alocada.

7. CONTROLE DE VERSÃO COM GIT E GITHUB
--------------------------------------------------------------------------------
Para vincular este projeto local ao seu repositório do GitHub e realizar o controle 
de alterações a cada melhoria realizada, utilize os comandos abaixo no terminal 
(certifique-se de ter o Git instalado no seu PC):

A. Configuração Inicial (Executar apenas na primeira vez):
---------------------------------------------------------
1. Inicializar o repositório Git local na pasta do projeto:
   > git init

2. Vincular este repositório local ao seu GitHub remoto:
   > git remote add origin https://github.com/programasindustriais13/painel_manutencao.git

3. Configurar sua identidade no Git (caso ainda não tenha configurado no seu PC):
   > git config --global user.name "Seu Nome"
   > git config --global user.email "seu_email@exemplo.com"

4. Adicionar todos os arquivos ao controle de versão (o arquivo `.gitignore` que 
   criei impedirá automaticamente o envio de arquivos temporários, da pasta `venv/` 
   e do banco local de testes `db.sqlite3` para a nuvem):
   > git add .

5. Realizar o primeiro commit de registro:
   > git commit -m "Estrutura da pesquisa de maquinas"

6. Definir o nome da branch principal como main:
   > git branch -M main

7. Enviar o código pela primeira vez para o GitHub:
   > git push -u origin main

B. Rotina diária de melhorias (A cada nova alteração que você fizer):
-------------------------------------------------------------------
Sempre que você fizer alterações nos templates, views ou scripts e quiser subir
a nova versão para o GitHub:

1. Adicione os arquivos alterados:
   > git add .

2. Faça o commit descrevendo o que foi implementado:
   > git commit -m "Melhoria: Regras de Finalização em Pausa"

3. Envie para o GitHub:
   > git push origin main


8. BUSCADOR DINÂMICO NO CAMPO DE SELEÇÃO DE MÁQUINAS (FILTRO LOCAL EM MEMÓRIA)
--------------------------------------------------------------------------------
Para lidar com uma lista extensa de equipamentos (importados do Excel), foi
implementada uma busca por digitação (autocomplete) local/em memória no campo de seleção de máquinas.

A. Modificações no Backend:
--------------------------
1. O formulário Django `StartServiceForm` em `forms.py` carrega a lista completa de
   máquinas através do queryset `Machine.objects.select_related('setor').order_by('nome')`
   para renderizar todas as opções diretamente nas tags `<option>` do HTML.
2. A utilização de `select_related('setor')` otimiza o carregamento evitando o problema de
   consultas adicionais (N+1) no banco de dados.
3. O texto exibido para o usuário mostra a combinação máquina + setor no formato:
   `"Nome da Máquina [Setor: Nome do Setor]"`.

B. Modificações no Frontend (Templates):
---------------------------------------
1. No layout base `base.html`, o bloco `extra_css` foi realocado para fora da
   tag `<style>`, possibilitando a inclusão de tags `<link>` de folha de estilo CDN.
2. Na tela de "Controle de Técnicos" (`technician_management.html`):
   - Foram integrados os links CDN oficiais (CSS e JS) da biblioteca **Tom Select**.
   - O Tom Select é inicializado em modo estático local no campo `#id_maquina`:
     * O dropdownParent está definido como `'body'` para evitar que o dropdown seja cortado
       pelas bordas/overflow do modal do Bootstrap.
     * **Exibição Imediata:** Ao clicar no campo, todas as opções carregadas aparecem imediatamente,
       permitindo ao operador rolar a lista ou digitar para filtrar em tempo real no próprio frontend.
     * Ao reabrir o modal "Iniciar Serviço" para qualquer técnico, a seleção anterior e a busca
       são limpas automaticamente.

C. Correção de Sintaxe do CSS Exposto:
-------------------------------------
1. Como o bloco `extra_css` em `base.html` foi movido para fora da tag `<style>` global, os templates
   `login.html` e `tv_dashboard.html` que sobrescreviam este bloco começaram a renderizar suas regras de CSS
   bruto como texto puro no topo da página.
2. Este problema foi corrigido com sucesso encapsulando explicitamente as regras de CSS destes templates
   dentro de tags `<style></style>` válidas no bloco `extra_css`.
3. Com isso, todo o vazamento de texto foi removido, o layout do Painel voltou ao normal e a inicialização
   do JavaScript do buscador (Tom Select) ocorre perfeitamente sem erros de travamento.


9. SUPORTE A MÚLTIPLAS ALOCAÇÕES E SERVIÇOS PAUSADOS
--------------------------------------------------------------------------------
Esta seção descreve a lógica de concorrência de alocações implementada para permitir
que um técnico acumule mais de um serviço em aberto ao mesmo tempo.

A. Regras de Negócio:
---------------------
- Um técnico pode ter NO MÁXIMO 1 alocação com status 'EM_ATENDIMENTO' por vez.
- Um técnico pode ter ILIMITADAS alocações com status 'EM_PAUSA' simultaneamente.
- O status do model Technician (.status) sempre reflete o estado do serviço ativo:
  * 'EM_ATENDIMENTO' → há exatamente 1 alocação aberta com status='EM_ATENDIMENTO'
  * 'EM_PAUSA'       → não há ativo, mas há ao menos 1 pausada
  * 'OCIOSO'         → não há nenhuma alocação aberta (todas encerradas com data_fim)

B. Novo Campo no Model Allocation (models.py):
----------------------------------------------
- Adicionado o campo `status` (CharField) no model Allocation com as seguintes opções:
  * 'EM_ATENDIMENTO' (padrão ao criar)
  * 'EM_PAUSA'
- Migração gerada: `maintenance/migrations/0002_allocation_add_status.py`
- Comando para reaplicar se necessário: python manage.py migrate

C. Propriedades Corrigidas/Adicionadas em Technician (models.py):
-----------------------------------------------------------------
- `active_allocation`: filtra allocations onde data_fim IS NULL AND status='EM_ATENDIMENTO'
  (antes filtrava apenas data_fim IS NULL — podia retornar pausadas erroneamente)
- `paused_allocations`: nova propriedade que retorna QuerySet de alocações onde
  data_fim IS NULL AND status='EM_PAUSA', ordenadas por data_pausa

D. Lógica das Views (views.py):
--------------------------------
- `start_service`: NÃO bloqueia se o técnico tiver apenas serviços pausados. Bloqueia
  SOMENTE se já houver uma alocação EM_ATENDIMENTO ativa (technician.active_allocation is not None).
- `pause_service`: seta allocation.status='EM_PAUSA' na alocação ativa e recalcula
  o status do técnico (vai para 'EM_PAUSA' se não houver outra ativa restante).
- `resume_service`: retoma a alocação pausada mais antiga — caso simples (sem ativa).
- `finish_service`: finaliza a alocação EM_ATENDIMENTO e recalcula status do técnico
  (EM_PAUSA se restarem pausadas, OCIOSO se não restar nenhuma).
- `resume_paused_allocation(allocation_id)` [NOVA]: troca de contexto — ativa a alocação
  pausada selecionada; se havia uma EM_ATENDIMENTO, ela vai automaticamente para EM_PAUSA
  com motivo "Interrompido para retomada de outro serviço."
- `finish_allocation(allocation_id)` [NOVA]: finaliza uma alocação ESPECÍFICA (ativa ou pausada)
  pelo seu ID. Recalcula o status do técnico com base nas alocações restantes.

E. Novas Rotas de URL (urls.py):
---------------------------------
  allocations/<int:allocation_id>/resume/  → resume_paused_allocation (name='resume_paused_allocation')
  allocations/<int:allocation_id>/finish/  → finish_allocation         (name='finish_allocation')

F. Interface do Operador (technician_management.html):
------------------------------------------------------
- Card do técnico redesenhado:
  * Seção "Serviço Ativo" exibe a alocação EM_ATENDIMENTO.
  * Badge "+N pausado(s)" aparece no header se houver alocações pausadas.
  * Seção colapsável "Serviços Pausados (N)" lista cada alocação pausada com:
    - Nome da máquina, criticidade, tempo decorrido, atividade, motivo da pausa.
    - Botão "Retomar" → POST para resume_paused_allocation/<allocation_id>/ (troca de contexto).
    - Botão "Finalizar" → abre modal #finishAllocModal com action apontando para
      finish_allocation/<allocation_id>/.
  * Botão "Iniciar Serviço" visível mesmo quando o técnico tem serviços pausados (mas não ativo).
- Modal #finishAllocModal: equivalente ao #finishModal, mas dinâmico por allocation_id.
  O campo action do form é setado via JavaScript ao abrir o modal.


10. ALTERAÇÃO VISUAL: COR DO STATUS OCIOSO NO PAINEL TV (2026-06-16)
--------------------------------------------------------------------------------
Arquivo modificado: maintenance/templates/maintenance/tv_dashboard.html

Alteração realizada:
- .tech-card.status-ocioso → background #1a0505 (preto avermelhado), border #7f1d1d;
  opacity: 0.72 removida (garante máximo contraste com o texto vermelho).
- Barra lateral esquerda (::before) → #dc3545 com box-shadow vermelho sutil.
- .tech-name dentro do card ocioso → color: #dc3545 (vermelho forte, font-weight: 700).
- .badge-ocioso → background #3b0a0a, color #dc3545, border 1px solid #7f1d1d.

Objetivo: chamar atenção visual imediata para técnicos ociosos na TV da fábrica.
Nenhum arquivo de backend (views.py, models.py, forms.py) foi alterado.


11. STATUS DE AUSÊNCIA E DISPONIBILIDADE DO TÉCNICO
--------------------------------------------------------------------------------
Esta seção documenta a expansão dos status do modelo Technician para contemplar
estados de ausência e situações de escala fora da fábrica.

A. Novos Status Adicionados (models.py — Technician.STATUS_CHOICES):
---------------------------------------------------------------------
Código              | Rótulo                              | Situação
--------------------|-------------------------------------|------------------------------
OCIOSO              | Disponível (Ocioso)                 | Técnico na fábrica, disponível
EM_ATENDIMENTO      | Em Atendimento                      | Serviço ativo em campo
EM_PAUSA            | Em Pausa                            | Serviço interrompido
AUSENTE_FOLGA       | Ausente – Folga/Escala              | Folga ou escala no dia
AUSENTE_FERIAS      | Ausente – Férias                    | Em período de férias
AUSENTE_MEDICO      | Ausente – Licença Médica/Afastamento| Afastado por saúde
EXTERNO_PLANTAO     | Plantão Fora da Fábrica             | Plantão em outra unidade

B. Constante de Controle (Technician.STATUS_AUSENCIA):
------------------------------------------------------
O model define o atributo de classe:
    STATUS_AUSENCIA = {'AUSENTE_FOLGA', 'AUSENTE_FERIAS', 'AUSENTE_MEDICO', 'EXTERNO_PLANTAO'}

E a propriedade:
    @property is_ausente → bool   (True se status ∈ STATUS_AUSENCIA)

C. Regras de Negócio:
--------------------
- Técnico com status de ausência NÃO pode receber novas ordens de serviço.
  A view start_service bloqueia a operação e exibe mensagem de erro.
- Serviços em EM_PAUSA existentes são MANTIDOS congelados no histórico; os
  botões "Retomar" ficam desabilitados enquanto o técnico estiver ausente.
- O status EM_ATENDIMENTO não pode ser definido manualmente via widget de
  disponibilidade — ele é controlado exclusivamente pelas views start/pause/finish.
- Um técnico EM_ATENDIMENTO não pode ser marcado como ausente diretamente;
  o operador deve primeiro pausar ou finalizar o serviço ativo.

D. Nova View e Rota (views.py / urls.py):
-----------------------------------------
- View: set_availability(request, technician_id)
- URL : POST /technicians/<id>/availability/   (name='set_availability')
- Aceita: OCIOSO, AUSENTE_FOLGA, AUSENTE_FERIAS, AUSENTE_MEDICO, EXTERNO_PLANTAO
- Para retornar o técnico ao fluxo normal, selecionar "Disponível (Ocioso)".

E. Interface do Operador (technician_management.html):
------------------------------------------------------
- Novos badges coloridos no header de cada card:
    FOLGA/ESCALA    → cinza-médio  (#6b7280)
    FÉRIAS          → ciano/teal   (#0e7490)
    LIC. MÉDICA     → roxo         (#7c3aed)
    PLANTÃO EXTERNO → laranja-brun (#c2410c)
- Card de aviso visual (ícone + texto) no corpo do card quando o técnico está ausente.
- Widget "Definir Escala / Disponibilidade" (mini-dropdown + botão Salvar) no rodapé
  de cada card, visível em todos os estados exceto EM_ATENDIMENTO.
- Botão "Iniciar Serviço" completamente oculto para técnicos ausentes.
- Botão "Retomar" desabilitado (cinza, lock icon) para pausas enquanto ausente.

F. Dashboard (views.py — dashboard):
-------------------------------------
- Novo KPI: absent_techs (contagem de todos com status ∈ STATUS_AUSENCIA)
- Gráfico de distribuição atualizado para incluir "Ausente/Externo" como fatia separada.

G. Migração de Banco:
--------------------
- Migração: 0003_technician_expand_status_choices.py
- Tipo: alteração apenas de metadados (choices) — SEM DDL no banco SQLite.
- Dados existentes são 100% preservados.
- Comando: python manage.py migrate


12. HABILITAÇÃO E CONFIGURAÇÃO DO DJANGO ADMIN (2026-06-16)
--------------------------------------------------------------------------------
Este painel estende o Django Admin padrão para permitir a visualização de logs 
e a gestão direta dos dados cadastrados e de histórico de alocações.

A. Registro de Modelos no Admin (admin.py):
------------------------------------------
Todos os modelos foram registrados com layouts otimizados e recursos de busca e filtros:
- Sector (Setor): Exibe o nome. Permite busca por nome.
- Machine (Máquina): Exibe nome, setor e criticidade. Possui filtros rápidos por setor e por criticidade. Permite busca por nome da máquina e do setor.
- Technician (Técnico): Exibe nome, matrícula e status. Filtro por status atual e busca por nome e matrícula.
- Allocation (Alocação): Exibe técnico, máquina, status, data de início, data de pausa e data de finalização. Filtros rápidos por datas e status, e busca pelo nome do técnico.

B. Logs de Alterações / Auditoria:
----------------------------------
- O aplicativo padrão `django.contrib.admin` e `django.contrib.contenttypes` estão ativos no settings.py.
- O bloco nativo de "Ações recentes" (LogEntry do Django) está visível na home do Django Admin (/admin/), registrando quem realizou criação, alteração ou exclusão de qualquer registro.
- Para facilitar a auditoria completa, o modelo LogEntry foi registrado diretamente no painel admin como uma tabela pesquisável e filtrável (apenas leitura).

C. Acesso ao Admin:
-------------------
- URL de Acesso: /admin/
- Usuário e senha padrão: Criados anteriormente (conforme seção 2).


13. HISTÓRICO MULTIPLO DE PAUSAS E RETORNOS (RELACIONAL)
--------------------------------------------------------------------------------
Para evitar a perda de dados ao retomar um atendimento e permitir múltiplos registros
de pausa em uma única alocação, foi criada uma tabela relacional de histórico.

A. Novo Modelo de Banco de Dados (HistoricoPausa):
--------------------------------------------------
Campos definidos no model HistoricoPausa:
- alocacao     -> ForeignKey para o model Allocation (related_name='pausas')
- data_pausa   -> DateTimeField (registra o momento de início da pausa)
- data_retorno -> DateTimeField (nulo ao pausar, preenchido com timezone.now() ao retomar)
- motivo_pausa -> TextField (obrigatório, contendo o motivo da pausa)

B. Comportamento nas Views (views.py):
-------------------------------------
- Pausar: Cria um registro no HistoricoPausa com o timestamp atual e o motivo. Altera o status da alocação para 'EM_PAUSA'.
- Retomar: Localiza o último registro no HistoricoPausa onde 'data_retorno' é nulo e preenche com a data/hora atual. Altera o status para 'EM_ATENDIMENTO'.
- Concluir: Além de definir 'data_fim' no registro principal de Allocation, se houver qualquer pausa em aberto, a data_retorno da pausa é preenchida automaticamente com o mesmo horário de fim de serviço, fechando o ciclo de forma consistente.

C. Exibição no Django Admin (admin.py):
---------------------------------------
- Foi implementado um TabularInline (HistoricoPausaInline) na classe AlocacaoAdmin.
- Ao visualizar ou editar uma Alocação antiga, uma tabela completa e elegante do histórico de pausas do técnico é renderizada diretamente na página de detalhes.


14. OTIMIZAÇÃO DA TAXA DE REFRESH E PERFORMANCE DO PAINEL TV (2026-06-16)
--------------------------------------------------------------------------------
Arquivos modificados: 
- maintenance/templates/maintenance/tv_dashboard.html
- maintenance/views.py
- maintenance/models.py

Alterações realizadas:
A. Tempo de Refresh do Painel (Frontend):
- Redução da taxa de atualização do Painel TV (/tv/) de 30 segundos para 10 segundos.
- Atualização do indicador no cabeçalho (de "↻ 30s" para "↻ 10s").
- Atualização do script JavaScript (REFRESH_SECS = 10) para recarregar a tela a cada 10 segundos.

B. Otimização de Performance (Backend):
- Uso de Prefetch e select_related/prefetch_related na view `tv_dashboard` para carregar 
  apenas as alocações ativas com suas respectivas máquinas, setores e histórico de pausas.
- Ajuste nas propriedades `active_allocation` (Technician) e `tempo_decorrido_str` 
  (Allocation) para consultar o prefetch cache (`_prefetched_objects_cache`) em memória.
  Isso evita problemas de N+1 consultas ao banco de dados SQLite, reduzindo a carga do servidor.


15. REDIRECIONAMENTO DINÂMICO PÓS-LOGIN POR PERFIL DE USUÁRIO (2026-06-16)
--------------------------------------------------------------------------------
Arquivos modificados:
- maintenance_project/settings.py
- maintenance/views.py
- maintenance/tests.py

Alterações realizadas:
A. Configuração de Redirecionamento (settings.py):
- Ajustada a variável LOGIN_REDIRECT_URL para 'home_redirect' para passar a usar a view intermediária de redirecionamento dinâmico ao invés do valor estático 'tv_dashboard'.

B. Lógica de Validação e Redirecionamento (views.py):
- A view `home_redirect` foi modificada para validar o perfil/grupo do usuário ou seu nome de usuário.
- Se o usuário pertencer ao grupo 'Visualizador' ou se seu username for exatamente 'tv', ele é redirecionado para a tela do painel de TV (`/tv/`).
- Para todos os outros perfis (como Operadores, Gestores e Administradores), o usuário é redirecionado automaticamente para o dashboard analítico (`/dashboard/`).
- Ambas as telas `/tv/` e `/dashboard/` exigem login do usuário por meio dos decoradores `@login_required` e `@operador_required` respectivamente, garantindo a integridade e segurança de cada ambiente.

C. Validação e Testes Unitários (tests.py):
- Adicionado o caso de teste `test_post_login_redirect` que simula logins de usuários com perfil de 'Visualizador', usuário com username 'tv', e usuários comuns/operadores, garantindo a conformidade e prevenindo regressões futuras.

D. Como testar os diferentes comportamentos:
- Para testar o comportamento do Visualizador (TV):
  1. Crie um grupo com o nome exato 'Visualizador' no admin do Django e associe um usuário a ele, OU crie um usuário com o username 'tv'.
  2. Faça login com este usuário. O sistema deverá abrir diretamente na tela de TV (/tv/).
- Para testar o comportamento dos demais perfis (Operadores/Gestores/Admin):
  1. Faça login com qualquer outro usuário (ex: admin padrão com senha @Freedom1234, ou usuário do grupo 'Operador').
  2. O sistema deverá abrir diretamente no dashboard de análise (/dashboard/).


16. LIMPEZA DE MÉTRICAS NO DASHBOARD (2026-06-16)
--------------------------------------------------------------------------------
Arquivos modificados:
- maintenance/templates/maintenance/dashboard.html
- maintenance/views.py
- Instrucoes.txt

Alterações realizadas:
A. Remoção da métrica "Máquinas Paradas":
- O card correspondente a "Máquinas Paradas" foi removido da página de dashboard (/dashboard/).
- Para balancear o layout do grid do Bootstrap com as 3 métricas restantes (Total Técnicos, Em Atendimento, Em Pausa), as colunas dos cards correspondentes foram ajustadas de `col-6 col-lg-3` para `col-12 col-md-4`. Isso faz com que os cards preencham a tela de maneira uniforme e harmônica em resoluções de tela a partir de tablets (md), e fiquem empilhados de forma limpa (col-12) em telas menores.

B. Otimização no Backend (Processamento do Servidor):
- A consulta ao banco de dados que calculava a contagem de máquinas paradas (`machines_in_maintenance`) na view `dashboard` do arquivo `maintenance/views.py` foi comentada.
- O campo correspondente no dicionário `context` enviado para o template também foi comentado para poupar recursos do servidor e reduzir a carga de processamento das requisições ao dashboard.


17. HISTÓRICO DE ALTERAÇÃO DE ESCALAS E AUDITORIA (2026-06-16)
--------------------------------------------------------------------------------
Implementação de trilha de auditoria para cada alteração de status de escala/
disponibilidade de técnicos, com exibição completa no Django Admin.

Arquivos modificados:
- maintenance/models.py
- maintenance/views.py
- maintenance/admin.py
- maintenance/migrations/0006_historico_escala.py  [NOVO]
- Instrucoes.txt

A. Novo Modelo de Banco de Dados (HistoricoEscala — models.py):
---------------------------------------------------------------
Tabela: maintenance_historicoescala

Campos:
  id                  → PK automática (BigAutoField)
  tecnico_id          → ForeignKey para Technician (related_name='historico_escalas')
                        CASCADE: se o técnico for excluído, o histórico vai junto.
  status_definido     → CharField(max_length=20) — código do status atribuído
                        (ex: 'AUSENTE_FOLGA', 'OCIOSO', 'AUSENTE_FERIAS', etc.)
  data_alteracao      → DateTimeField(auto_now_add=True) — momento exato da mudança,
                        preenchido automaticamente pelo banco; não editável.
  usuario_responsavel → ForeignKey para auth.User (SET_NULL, null=True, blank=True)
                        Registra qual operador logado realizou a alteração.
                        SET_NULL: se o usuário for excluído, o registro é mantido
                        com o campo nulo (preservação do histórico).

Método auxiliar:
  get_status_definido_display_label() → retorna o rótulo legível do status
  utilizando Technician.STATUS_CHOICES como referência de tradução.

Meta:
  ordering = ['-data_alteracao']  (mais recentes primeiro)

B. Registro Automático (views.py — set_availability):
------------------------------------------------------
- Após o técnico ter seu status salvo com sucesso, a view set_availability
  cria automaticamente um novo registro HistoricoEscala com:
    * tecnico        → o técnico que foi alterado
    * status_definido → o novo status aplicado (ex: 'AUSENTE_FOLGA')
    * usuario_responsavel → request.user (operador autenticado na sessão)
- Nenhuma ação manual é necessária: o registro ocorre de forma transparente
  sempre que o widget de disponibilidade for usado na tela do operador (/technicians/).

C. Exibição no Django Admin (admin.py):
----------------------------------------
1. HistoricoEscalaInline — embutido na ficha de cada Técnico (TecnicoAdmin):
   - Tipo: TabularInline (leitura apenas, sem botão de adição ou exclusão).
   - Colunas: Status Definido (rótulo legível), Data/Hora da Alteração, Usuário Responsável.
   - Ordenação: mais recentes no topo (-data_alteracao).
   - Como acessar: /admin/maintenance/technician/<id>/change/
     → Role para o final da página para ver a tabela "Histórico de Escalas".

2. HistoricoEscalaAdmin — listagem global dedicada (somente leitura):
   - URL: /admin/maintenance/historicoescala/
   - Colunas da listagem: Técnico, Status Definido, Data/Hora da Alteração, Usuário Responsável.
   - Filtros laterais: por data_alteracao (hierarquia de data) e por status_definido.
   - Campo de busca: nome e matrícula do técnico.
   - Hierarquia de datas (date_hierarchy): navega por ano → mês → dia.
   - Permissões: add=False, change=False, delete=False (registro de auditoria imutável).

D. Migração de Banco:
---------------------
- Arquivo: maintenance/migrations/0006_historico_escala.py
- Operação: CREATE TABLE — não altera nenhuma tabela existente.
- Dados existentes: 100% preservados.
- Comandos executados:
    python manage.py makemigrations maintenance --name historico_escala
    python manage.py migrate
- Resultado: maintenance.0006_historico_escala... OK

E. Regras de Segurança e Integridade:
--------------------------------------
- O campo data_alteracao usa auto_now_add=True, portanto é imutável após criação.
- O registro HistoricoEscala não pode ser criado, alterado ou excluído via Admin
  (apenas visualizado), garantindo a integridade da trilha de auditoria.
- Se o Usuário responsável for removido do sistema, o registro de auditoria é
  mantido com usuario_responsavel = NULL (comportamento SET_NULL).
- Se o Técnico for excluído, todos os seus registros de histórico de escala também
  são excluídos em cascata (CASCADE), evitando registros órfãos no banco.


18. EXPORTAÇÃO DE RELATÓRIO EXCEL NO DASHBOARD (2026-06-16)
--------------------------------------------------------------------------------
Permite ao gestor baixar um relatório completo de todas as alocações registradas
no sistema, diretamente pela tela de Dashboard (/dashboard/).

Arquivos modificados:
- maintenance/views.py
- maintenance/urls.py
- maintenance/templates/maintenance/dashboard.html
- requirements.txt  (já continha openpyxl>=3.1.0)
- Instrucoes.txt

Biblioteca necessária (já no requirements.txt):
  openpyxl >= 3.1.0
  → Instalar (se necessário): pip install openpyxl>=3.1.0

A. Nova View de Exportação (views.py — exportar_relatorio_excel):
------------------------------------------------------------------
- Protegida pelo decorador @operador_required (exige login + grupo Operador/Staff).
- Consulta todas as alocações usando:
    Allocation.objects.select_related('tecnico', 'maquina', 'maquina__setor')
                      .prefetch_related('pausas')
                      .order_by('-data_inicio')
  (otimização: nenhuma consulta N+1 ao banco de dados)
- Gera a planilha Excel em memória (io.BytesIO) — sem gravação em disco.
- Retorna HttpResponse com:
    Content-Type : application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    Content-Disposition: attachment; filename="relatorio_manutencao_AAAAMMDD_HHMM.xlsx"

B. Estrutura da Planilha Gerada:
---------------------------------
Linha 1  → Título mesclado (A1:J1) com data/hora da exportação.
Linha 2  → Cabeçalho (fundo azul escuro #1E3A5F, texto branco, negrito).
Linha 3+ → Uma linha por alocação, com alternância de fundo (linhas pares em #EBF0F8).

Colunas exportadas (10 no total):
  Col A  → Técnico (nome completo)
  Col B  → Matrícula
  Col C  → Setor (nome do setor da máquina)
  Col D  → Máquina (nome da máquina)
  Col E  → Criticidade (Baixa / Média / Alta)
  Col F  → Status (Em Atendimento / Em Pausa / Concluído)
  Col G  → Data de Início (DD/MM/AAAA HH:MM)
  Col H  → Data de Fim (DD/MM/AAAA HH:MM ou "—" se em aberto)
  Col I  → Qtd. Pausas (número de pausas no HistoricoPausa)
  Col J  → Tempo Total em minutos (descontando pausas; se em aberto, calcula até agora)

Funcionalidades extras da planilha:
  - Bordas finas em todas as células (cor #CCCCCC).
  - Cabeçalho congelado (freeze_panes = 'A3'): rolar os dados sem perder o cabeçalho.
  - Larguras de colunas pré-definidas para boa legibilidade sem ajuste manual.

C. Nova Rota de URL (urls.py):
--------------------------------
  URL  : /dashboard/exportar-excel/
  Name : exportar_relatorio_excel
  View : views.exportar_relatorio_excel

D. Botão no Dashboard (dashboard.html):
-----------------------------------------
- Botão Bootstrap verde (btn-success btn-lg) com ícone bi-file-earmark-excel-fill.
- Posicionado no cabeçalho do dashboard, à direita do título, responsivo (col-md-4).
- ID único para testes: id="btn-exportar-excel"
- Acessível via href (link GET simples); não usa AJAX — garante download nativo do browser.
- O download ocorre SEM deslogar o usuário e SEM recarregar o dashboard.

E. Segurança:
--------------
- Nenhuma tabela do banco de dados foi criada, alterada ou excluída.
- Somente usuários autenticados com permissão Operador/Staff/Superuser podem acessar.
- Tentativa de acesso não autorizado redireciona automaticamente para /tv/.


19. FILTRO DE PERÍODO UNIFICADO E RELATÓRIO EXCEL EXPANDIDO (2026-06-16)
--------------------------------------------------------------------------------
Implementação de um filtro por intervalo de datas no Dashboard que restringe
simultaneamente os gráficos exibidos na tela e os dados exportados para o Excel.
O relatório Excel também foi expandido para incluir o histórico completo de pausas,
dados do operador e observações textuais.

Arquivos modificados:
- maintenance/views.py
- maintenance/templates/maintenance/dashboard.html
- Instrucoes.txt

NENHUMA MIGRAÇÃO necessária — apenas filtros de QuerySet (sem alteração nos models).

A. Filtro de Período no Dashboard (Frontend — dashboard.html):
--------------------------------------------------------------
- Adicionado um formulário horizontal (GET) no topo do Dashboard (/dashboard/)
  com dois campos de data: "Data Inicial" e "Data Final".
- Botão "Filtrar" (btn-primary) submete o formulário e recarrega a página com
  os parâmetros ?data_inicio=YYYY-MM-DD&data_final=YYYY-MM-DD na URL.
- Link "Limpar" (btn-outline-secondary) aponta para /dashboard/ sem parâmetros,
  restaurando o padrão retroativo de 30 dias.
- Badge indicativo exibe o período ativo em formato DD/MM/AAAA → DD/MM/AAAA.
- Após o filtro, os inputs mantêm as datas escolhidas (repopulados pelo contexto).

IDs dos elementos para testes:
  id="form-filtro-periodo"   → formulário de filtro
  id="id_data_inicio"        → input Data Inicial
  id="id_data_final"         → input Data Final
  id="btn-filtrar-periodo"   → botão Filtrar
  id="btn-limpar-filtro"     → link Limpar
  id="btn-exportar-excel"    → botão Exportar Excel (href atualizado com datas)

B. Lógica na View do Dashboard (views.py — dashboard):
------------------------------------------------------
Parâmetros GET aceitos:
  data_inicio  → string YYYY-MM-DD   (formato nativo do input type="date")
  data_final   → string YYYY-MM-DD

Comportamento e validação:
  1. Captura os valores com request.GET.get('data_inicio', '').strip().
  2. Tenta converter com datetime.date.fromisoformat().
  3. Em caso de string inválida ou ausente, cai no padrão retroativo (30 dias atrás
     para data_inicio, data de hoje para data_final) — nunca gera exceção.
  4. Se data_inicio > data_final, as datas são trocadas automaticamente.
  5. As strings no formato YYYY-MM-DD são passadas ao contexto como
     'data_inicio_str' e 'data_final_str' para repopular os inputs do formulário.

O que É filtrado por período:
  - Gráfico "Manutenções por Criticidade" → conta Allocations com
    data_inicio__date__range=[data_inicio, data_final] filtradas por criticidade.
  - Gráfico "Volume de Atendimentos por Setor" → mesma lógica, filtrada por setor.

O que NÃO é filtrado (estado atual, snapshot em tempo real):
  - KPI cards: Total Técnicos, Em Atendimento, Em Pausa, Ausentes/Externos.
  - Gráfico de distribuição de status dos técnicos (rosca).
  Justificativa: esses indicadores refletem o estado AGORA da fábrica —
  não têm semântica temporal de "quantos estavam assim naquele período".

C. Filtro na Exportação Excel (views.py — exportar_relatorio_excel):
--------------------------------------------------------------------
- A view de exportação captura os mesmos parâmetros GET (data_inicio, data_final)
  com a mesma lógica de validação e fallback.
- O queryset aplica:
    Allocation.objects.filter(data_inicio__date__range=[data_inicio, data_final])
- Quando o usuário clica "Exportar para Excel" no Dashboard, o href já contém
  automaticamente os parâmetros do filtro ativo:
    /dashboard/exportar-excel/?data_inicio=YYYY-MM-DD&data_final=YYYY-MM-DD
- O Excel baixado contém APENAS as alocações do período selecionado.
- Nome do arquivo inclui o período: relatorio_manutencao_AAAAMMDD-AAAAMMDD_AAAAMMDD_HHMM.xlsx

D. Estrutura Expandida do Relatório Excel (13 colunas):
-------------------------------------------------------
Linha 1  → Título mesclado A1:M1 com período e data/hora da exportação.
Linha 2  → Cabeçalho (fundo azul escuro #1E3A5F, texto branco, negrito).
Linha 3+ → Uma linha por alocação, com alternância de fundo (linhas pares em #EBF0F8).

Colunas exportadas (13 no total):
  Col A  → Técnico (nome completo)
  Col B  → Matrícula
  Col C  → Setor (nome do setor da máquina)
  Col D  → Máquina (nome da máquina)
  Col E  → Criticidade (Baixa / Média / Alta)
  Col F  → Operador (nome completo ou username de quem realizou a alocação)
  Col G  → Status (Em Atendimento / Em Pausa / Concluído)
  Col H  → Data/Hora de Início (DD/MM/AAAA HH:MM)
  Col I  → Data/Hora de Término (DD/MM/AAAA HH:MM ou "—" se em aberto)
  Col J  → Tempo Total em minutos (descontando pausas; se em aberto, calcula até agora)
  Col K  → Observação Inicial do Serviço (campo atividade_observacao)
  Col L  → Observação de Conclusão do Serviço (campo observacao_conclusao)
  Col M  → Histórico de Pausas (pausas concatenadas com quebras de linha no formato:
            "↓ DD/MM/AAAA HH:MM  →  DD/MM/AAAA HH:MM | Motivo da Pausa"
            Uma linha por pausa. Se não há pausas, exibe "—".)

Funcionalidades extras da planilha:
  - Cabeçalho e linha de título mesclados automaticamente.
  - Cabeçalho congelado em A3 (scroll vertical sem perder o cabeçalho).
  - Bordas finas em todas as células (#CCCCCC).
  - Larguras pré-definidas para cada coluna (sem necessidade de ajuste manual).
  - Altura dinâmica das linhas: calculada com base na quantidade de pausas da
    alocação (18 pixels × nº de pausas), garantindo que o texto não fique cortado.
  - Alinhamento à esquerda com wrap_text nas colunas de texto longo (A, C, D, F, G, K, L, M).
  - Alinhamento centralizado nas colunas numéricas e de datas (B, E, H, I, J).

E. Segurança e Integridade:
---------------------------
- Nenhuma tabela do banco de dados foi criada, alterada ou excluída.
- Somente usuários autenticados com permissão Operador/Staff/Superuser podem acessar.
- Tentativa de acesso não autorizado redireciona automaticamente para /tv/.
- Strings de data inválidas nunca travam a aplicação (sempre há fallback seguro).
- O Excel é gerado em memória (io.BytesIO) — sem gravação em disco.


20. CRIAÇÃO DA CONSTITUIÇÃO E DIRETRIZES DE BLINDAGEM DO SISTEMA (2026-06-17)
--------------------------------------------------------------------------------
Arquivo criado: constitution.md na raiz do projeto.

Este arquivo documenta as diretrizes essenciais para:
- Princípio da Não-Destruição de Dados (migrações seguras, campos opcionais e tabelas de históricos incremental);
- Preservação das Regras de Negócio Críticas (concorrência de técnicos, histórico relacional de pausas, otimização da tela da TV e rastreabilidade/auditoria de ações);
- Limitações de Escopo Técnico (sem novos apps Django ou venv desnecessários, fallbacks de arquivos nulos e filtros unificados no dashboard);
- Protocolo de documentação no arquivo Instrucoes.txt ao final de cada tarefa;
- Reconhecimento obrigatório do agente de IA antes de realizar qualquer alteração futura no código.


21. VINCULAÇÃO DE USUÁRIOS A TÉCNICOS E PERMISSÕES DE CHÃO DE FÁBRICA (2026-06-17)
--------------------------------------------------------------------------------
Permite que técnicos tenham login próprio no sistema para gerenciar seu próprio
card de serviços. Um líder/operador pode gerenciar todos os cards e cadastros.

Arquivos modificados:
- maintenance/models.py
- maintenance/migrations/0008_technician_user_perfil.py  [NOVO]
- maintenance/forms.py
- maintenance/views.py
- maintenance/admin.py
- maintenance/templates/maintenance/technician_management.html
- maintenance/templates/maintenance/technician_form.html
- Instrucoes.txt

A. Novos Campos no Model Technician (models.py):
-------------------------------------------------
Campo                | Tipo                        | Descrição
---------------------|-----------------------------|---------------------------------
user                 | OneToOneField → auth.User   | Usuário Django vinculado (optional)
                     | SET_NULL, null=True, blank=True | Técnicos sem user: sem acesso
perfil               | CharField(max_length=10)    | 'TECNICO' ou 'OPERADOR'
                     | null=True, blank=True, default='TECNICO'

Choices do perfil:
  TECNICO  → Acessa apenas o próprio card em /management/
  OPERADOR → Acesso total (dashboard, cadastros e todos os cards)

Migração gerada: 0008_technician_user_perfil.py
Tipo: AddField apenas — dados existentes 100% preservados.

B. Como o Operador Líder Cria o Usuário de um Técnico:
------------------------------------------------------
1. Acesse o sistema com uma conta Operador/Admin.
2. Navegue até Cadastros → Técnicos → clique em "Editar" (ícone de lápis) no técnico desejado.
3. Na seção "Acesso ao Sistema" (parte inferior do formulário):
   a. Preencha o campo "Login (Username)": ex. 'joao.silva' ou 'tec001'.
   b. Preencha o campo "Senha de Acesso" com uma senha simples (mínimo 4 caracteres).
      ATENÇÃO: Os validadores complexos do Django (comprimento mínimo 8, histórico, etc.)
      estão DESATIVADOS para este formulário específico de chão de fábrica. Use senhas
      simples como o número de matrícula ou um PIN de 4-6 dígitos.
   c. Selecione o "Perfil de Acesso":
      - "Técnico" → o funcionário vê apenas o próprio card (sem acesso a dashboard ou cadastros).
      - "Operador/Líder" → acesso total (equivale a um gestor de fábrica).
4. Clique em "Salvar".
   → O sistema criará automaticamente o User Django com create_user() e o vinculará
     ao registro do Técnico através do campo OneToOneField.
   → Uma mensagem de confirmação aparecerá: "Usuário '<username>' criado e vinculado."

Para atualizar o usuário de um técnico já vinculado:
   - O campo "Login" vem pré-preenchido com o username atual.
   - Para trocar a senha: preencha o campo "Senha de Acesso" com a nova senha.
   - Para manter a senha: deixe o campo "Senha de Acesso" em branco.
   - Alterar o Perfil de Acesso entra em vigor imediatamente após o Salvar.

C. Como o Técnico Faz Login:
----------------------------
1. Acesse: /login/  (ou a URL raiz do sistema)
2. Informe o Username e Senha definidos pelo Operador.
3. O sistema redireciona automaticamente para /management/ (Controle de Técnicos).
4. Apenas o próprio card do técnico terá botões de ação (Iniciar, Pausar, Concluir).
5. Os cards dos colegas aparecem em modo de visualização somente-leitura.

D. Regras de Controle de Acesso por Perfil:
-------------------------------------------
Ação / Tela                          | Perfil TECNICO | Perfil OPERADOR
-------------------------------------|----------------|------------------
/management/ (ver todos os cards)    | ✅ Sim         | ✅ Sim
Iniciar/Pausar/Concluir próprio card | ✅ Sim         | ✅ Sim
Iniciar/Pausar/Concluir card alheio  | ❌ Não         | ✅ Sim
Widget "Definir Escala/Disponibil."  | ❌ Não         | ✅ Sim
/dashboard/                          | ❌ Redirecionado| ✅ Sim
/cruds/ (cadastros)                  | ❌ Redirecionado| ✅ Sim
/tv/ (painel TV)                     | ✅ Sim (login) | ✅ Sim

E. Decorators e Helpers Criados (views.py):
-------------------------------------------
- _user_is_operador(user)         → bool: True se superuser/staff/grupo Operador/perfil OPERADOR
- _get_technician_proprio(user)   → Technician | None: retorna o técnico vinculado ao usuário
- tecnico_or_operador_required    → decorator para /management/ (permite TECNICO + OPERADOR)
- operador_required               → refatorado: redireciona TECNICO para /management/ (antes ia para /tv/)

F. Migração de Banco:
---------------------
- Arquivo: maintenance/migrations/0008_technician_user_perfil.py
- Operações: AddField user (OneToOneField SET_NULL) + AddField perfil (CharField)
- Dados existentes: 100% preservados (ambos os campos são null=True, blank=True)
- Comandos executados:
    python manage.py makemigrations maintenance --name technician_user_perfil
    python manage.py migrate
- Resultado: maintenance.0008_technician_user_perfil... OK

G. Admin Django (/admin/):
--------------------------
- TecnicoAdmin atualizado: list_display e list_filter incluem 'perfil' e 'user'.
- Busca por username do usuário vinculado habilitada (search_fields inclui user__username).
- Para visualizar/editar vínculos: /admin/maintenance/technician/


22. PERFIL TÉCNICO LÍDER — NOVO NÍVEL DE ACESSO INTERMEDIÁRIO (2026-06-17)
--------------------------------------------------------------------------------
Adicionado o perfil TECNICO_LIDER ao sistema. Este perfil fica entre o Técnico comum
e o Operador/Administrador, sendo ideal para líderes de equipe que precisam gerenciar
o painel e visualizar métricas, mas não devem alterar cadastros base.

Arquivos modificados:
- maintenance/models.py          (PERFIL_CHOICES + max_length=15)
- maintenance/migrations/0009_perfil_max_length_tecnico_lider.py  [NOVO]
- maintenance/forms.py           (choice TECNICO_LIDER adicionada ao TechnicianForm)
- maintenance/views.py           (novos helpers + decorators + regras de acesso)
- maintenance/templates/maintenance/technician_management.html
- maintenance/templates/maintenance/technician_form.html
- Instrucoes.txt

A. Três Perfis de Acesso (campo perfil no model Technician):
------------------------------------------------------------
Valor         | Nome na UI             | Descrição
--------------|------------------------|------------------------------------------
TECNICO       | Técnico                | Acessa apenas o próprio card em /management/
TECNICO_LIDER | Técnico Líder          | Acessa /management/ (poder total) + /dashboard/
OPERADOR      | Operador/Administrador | Acesso total: /management/ + /dashboard/ + /cruds/

B. Tabela de Permissões por Tela:
----------------------------------
Tela / Ação                          | TECNICO  | TECNICO_LIDER | OPERADOR
-------------------------------------|----------|---------------|----------
/management/ (ver todos os cards)    | ✅ Sim   | ✅ Sim        | ✅ Sim
Iniciar/Pausar/Concluir próprio card | ✅ Sim   | ✅ Sim        | ✅ Sim
Iniciar/Pausar/Concluir card alheio  | ❌ Não   | ✅ Sim        | ✅ Sim
Widget "Definir Escala"              | ❌ Não   | ✅ Sim        | ✅ Sim
/dashboard/ (métricas + gráficos)    | ❌ Não   | ✅ Sim        | ✅ Sim
Exportar relatório Excel             | ❌ Não   | ✅ Sim        | ✅ Sim
/cruds/ (cadastros)                  | ❌ Não   | ❌ Não        | ✅ Sim
/tv/ (Painel TV)                     | ✅ Sim   | ❌ Não*       | ✅ Sim

* TECNICO_LIDER é bloqueado no /tv/ e redirecionado para /management/ com aviso.

C. Redirecionamento Pós-Login (home_redirect):
----------------------------------------------
- TECNICO       → /management/
- TECNICO_LIDER → /dashboard/
- OPERADOR      → /dashboard/
- Visualizador  → /tv/

D. Como Criar um Técnico Líder:
--------------------------------
1. Acesse o sistema como Operador/Administrador.
2. Vá em Cadastros → Técnicos → Editar o técnico desejado.
3. Na seção "Acesso ao Sistema", preencha:
   - Login (Username): ex. 'lider.joao'
   - Senha de Acesso: ex. '12345' (mínimo 4 caracteres)
   - Perfil de Acesso: selecione "Técnico Líder — Acesso ao painel e dashboard (sem cadastros)"
4. Clique em Salvar.

E. Helpers e Decorators Adicionados (views.py):
-----------------------------------------------
- _user_is_lider_ou_operador(user) → True para OPERADOR e TECNICO_LIDER
  (usado nas views de ação: start, pause, resume, finish, set_availability)

- @lider_ou_operador_required → decorator para /dashboard/ e /exportar_excel/
  Permite: OPERADOR e TECNICO_LIDER.
  Bloqueia: TECNICO (redireciona para /management/).

- @operador_required → restrito apenas a OPERADOR puro para /cruds/.
  TECNICO_LIDER é redirecionado para /management/ com mensagem.

- @tecnico_or_operador_required → mantido para /management/.
  Permite todos os perfis com user vinculado (TECNICO, TECNICO_LIDER, OPERADOR).

F. Migração de Banco:
----------------------
- Arquivo: maintenance/migrations/0009_perfil_max_length_tecnico_lider.py
- Operação: AlterField — max_length de 10 para 15 (para acomodar 'TECNICO_LIDER' = 13 chars)
- Dados existentes: 100% preservados (apenas aumenta tamanho de coluna)
- Comandos executados:
    python manage.py makemigrations maintenance --name perfil_max_length_tecnico_lider
    python manage.py migrate
- Resultado: maintenance.0009_perfil_max_length_tecnico_lider... OK


23. CORREÇÃO CRÍTICA DE FRONTEND: ALINHAMENTO DE PERMISSÕES DO TÉCNICO LÍDER NA UI (2026-06-17)
--------------------------------------------------------------------------------
Ajustadas as diretivas condicionais nos templates HTML para garantir a correta aplicação das permissões de Técnico Líder no painel de gerenciamento e na barra de navegação (navbar).

Arquivos modificados:
- maintenance/templates/maintenance/technician_management.html
- maintenance/templates/maintenance/base.html

A. Liberação de Ações em Cards Alheios e Escala no Painel:
- Alteradas as travas condicionais do template de `user_can_manage` para verificações diretas baseadas no perfil do técnico logado e no proprietário do card:
  - Botões de Ações (Iniciar/Pausar/Concluir):
    {% if request.user.technician_profile.perfil == 'OPERADOR' or request.user.technician_profile.perfil == 'TECNICO_LIDER' or tech.user == request.user %}
  - Widget "Definir Escala / Disponibilidade":
    {% if request.user.technician_profile.perfil == 'OPERADOR' or request.user.technician_profile.perfil == 'TECNICO_LIDER' %} (com status != 'EM_ATENDIMENTO' aninhado)

B. Menu de Navegação (Navbar) no base.html:
- Configurados os itens da navbar de forma alinhada à matriz de permissões:
  - Painel TV (Modo TV): Estritamente oculto para o perfil `TECNICO_LIDER`.
  - Dashboard: Visível para `TECNICO_LIDER` e `OPERADOR` (além de administradores e staff).
  - Cadastros: Estritamente oculto para o perfil `TECNICO_LIDER` e `TECNICO` (visível apenas para `OPERADOR` e staff).


24. REFATORAÇÃO DE ARQUITETURA: MIGRAÇÃO PARA SISTEMA NATIVO DE GRUPOS E PERMISSÕES DO DJANGO (2026-06-17)
--------------------------------------------------------------------------------
Migrada a lógica de permissões e controle de acesso para o sistema nativo de Grupos do Django (django.contrib.auth.models.Group), garantindo alta escalabilidade.

Arquivos modificados:
- maintenance/apps.py
- maintenance/views.py
- maintenance/templates/maintenance/technician_management.html
- maintenance/templates/maintenance/base.html
- maintenance/tests.py

A. Inicialização Automática de Grupos:
- Adicionada a função ready() no app config (apps.py) que garante que os seguintes grupos existam no banco de dados na inicialização:
  - 'Tecnicos'
  - 'Tecnicos_Lideres'
  - 'Operadores'

B. Lógica de Sincronização no Formulário:
- Ao salvar um técnico (technician_create e technician_edit), se um usuário estiver associado, limpa-se todos os seus grupos antigos (user.groups.clear()) e adiciona-se o grupo correspondente ao perfil selecionado:
  - 'TECNICO' -> Grupo 'Tecnicos'
  - 'TECNICO_LIDER' -> Grupo 'Tecnicos_Lideres'
  - 'OPERADOR' -> Grupo 'Operadores'

C. Decorators e Segurança de Views:
- Refatorados os decorators operador_required e lider_ou_operador_required para usar verificação de grupo nativo.
- View tv_dashboard (/tv/): Restrita aos grupos 'Operadores' ou visualizadores autorizados (grupo 'Visualizador' ou username 'tv'). Tentativas de acesso não autorizadas redirecionam para /management/ com Django Messages.

D. Regras de Interface Dinâmicas (Templates):
- As checagens nos templates HTML foram migradas para o uso nativo de grupos do Django:
  - Ações em cards: {% if request.user.groups.all.0.name == 'Operadores' or request.user.groups.all.0.name == 'Tecnicos_Lideres' or tech.user == request.user %}
  - Widget Escala: {% if request.user.groups.all.0.name == 'Operadores' or request.user.groups.all.0.name == 'Tecnicos_Lideres' %}
  - Navbar em base.html adaptado de forma análoga.
- Removido o link/botão "Ver Painel TV" do cabeçalho da tela de gerenciamento de técnicos (technician_management.html) e o link "Painel TV (Modo TV)" da barra de navegação superior (base.html) para os técnicos (grupos 'Tecnicos' e 'Tecnicos_Lideres'). O Painel TV agora só é visível na interface para membros do grupo 'Operadores', 'Visualizador' ou administradores.

25. AJUSTE DE USABILIDADE: LINK DE RETORNO DINÂMICO NO PAINEL TV (2026-06-17)
--------------------------------------------------------------------------------
Adicionado um atalho de navegação no título principal do Painel TV (/tv/) para 
redirecionamento dinâmico baseado no perfil do usuário logado.

Arquivos modificados:
- maintenance/templates/maintenance/tv_dashboard.html
- Instrucoes.txt

A. Comportamento do Link (HTML/CSS):
- O título principal "Status da Manutenção" foi envolvido por uma tag de link <a> apontando para a view `home_redirect`.
- CSS customizado (.tv-title-link) foi adicionado para remover o sublinhado padrão dos links (`text-decoration: none !important`), mantendo as cores, tamanhos e o gradiente originais do texto intactos.
- O cursor do mouse foi alterado para o formato de clique (mãozinha/`cursor: pointer`) ao passar por cima do título.

B. Redirecionamento Dinâmico (Lógica de Perfis):
- Usuário Operador ou Técnico Líder → Redirecionado de volta para o Dashboard (/dashboard/).
- Usuário genérico da TV (Visualizador) ou username 'tv' → Apenas recarrega a própria tela do Painel TV (/tv/).
- Se a sessão do usuário expirar, o link encaminhará o usuário para a tela de login.


26. CORREÇÃO DE BUG: VALIDAÇÃO OBRIGATÓRIA NA FINALIZAÇÃO DE ORDEM (2026-06-18)
--------------------------------------------------------------------------------
Corrigido bug em que ao tentar finalizar um serviço (ativo ou pausado) sem preencher
a observação de conclusão, o formulário falhava silenciosamente no backend (retornando
HTTP 200 em vez de redirecionar com 302). O usuário não recebia aviso de erro e o
serviço não era encerrado.

Arquivos modificados:
- maintenance/forms.py
- maintenance/templates/maintenance/technician_management.html
- Instrucoes.txt

NENHUMA migração necessária — apenas correção de validação de formulário e UX.
Nenhuma tabela ou model foi alterado.

A. Correção no Backend (forms.py — FinishServiceForm):
------------------------------------------------------
- O campo `observacao_conclusao` agora é explicitamente definido como obrigatório
  via `self.fields['observacao_conclusao'].required = True` no __init__().
- Adicionada mensagem de erro customizada:
  "A observação de conclusão é obrigatória para encerrar o serviço."
- Adicionado atributo HTML `required='required'` no widget Textarea para
  que o navegador também bloqueie o envio do formulário vazio (validação dupla).

B. Correção no Frontend — Modal "Concluir Serviço Ativo" (finishModal):
-----------------------------------------------------------------------
- Adicionado asterisco vermelho (*) no label do campo de observação de conclusão,
  indicando visualmente a obrigatoriedade.
- Adicionado bloco condicional Django para exibir erros inline:
  {% if finish_form.observacao_conclusao.errors %}
      <div class="text-danger small mt-1">
          {{ finish_form.observacao_conclusao.errors.0 }}
      </div>
  {% endif %}
- Adicionado texto auxiliar: "A observação de conclusão é obrigatória."

C. Correção no Frontend — Modal "Finalizar Serviço Pausado" (finishAllocModal):
-------------------------------------------------------------------------------
- Adicionado asterisco vermelho (*) no label do campo de observação.
- Adicionado atributo `required` na tag <textarea> manual do campo
  `observacao_conclusao`, impedindo envio vazio pelo navegador.

D. Persistência do Modal (JavaScript):
---------------------------------------
- Adicionado script que detecta mensagens de erro do Django na página
  contendo "observação", "obrigatória" ou "conclusão" e reabre
  automaticamente o modal #finishModal para que o técnico veja o erro
  sem a tela resetar.

E. Fluxo Corrigido:
--------------------
1. Técnico clica em "Concluir" → abre o modal.
2. Se a observação estiver vazia:
   a. Navegador bloqueia o envio (atributo required no HTML) → alerta nativo.
   b. Caso o HTML required seja contornado, o Django valida e retorna HTTP 200
      com mensagem de erro via Django Messages → modal reabre automaticamente.
3. Se a observação estiver preenchida:
   → Formulário válido → serviço encerrado → redirect 302 → mensagem de sucesso.


24. PROGRESSIVE WEB APP (PWA) — INSTALAÇÃO EM CELULARES E TABLETS (2026-06-19)
--------------------------------------------------------------------------------
O sistema foi transformado em um PWA para permitir a instalação em smartphones
e tablets no chão de fábrica, adquirindo propriedades de aplicativo nativo.

Arquivos criados:
- maintenance/static/maintenance/manifest.json            [NOVO]
- maintenance/static/maintenance/service-worker.js         [NOVO]
- maintenance/static/maintenance/pwa-icons/icon-192.png    [NOVO]
- maintenance/static/maintenance/pwa-icons/icon-512.png    [NOVO]

Arquivos modificados:
- maintenance/templates/maintenance/base.html
- maintenance/views.py
- maintenance/urls.py
- Instrucoes.txt

NENHUMA MIGRAÇÃO necessária — apenas arquivos estáticos e configuração de templates.

A. Manifesto do PWA (manifest.json):
--------------------------------------
Configuração do manifesto com:
  - name        : "Painel Manutenção — Gestão Industrial"
  - short_name  : "Manutenção"
  - start_url   : "/" (raiz do site)
  - display     : "standalone" (sem barra do navegador)
  - background_color : "#0f172a" (cor escura da tela de login)
  - theme_color      : "#1e1b4b" (gradiente da navbar)
  - Ícones de 192×192 e 512×512 pixels (PNG, purpose: any maskable)

B. Service Worker (service-worker.js):
---------------------------------------
Gerenciador de cache dos arquivos estáticos essenciais com estratégia híbrida:
  - Recursos CDN (Bootstrap CSS/JS, Bootstrap Icons, Google Fonts Inter, Chart.js):
    → Estratégia cache-first: usa o cache primeiro, busca da rede se não tiver.
  - Páginas dinâmicas do Django (HTML renderizado pelo servidor):
    → Estratégia network-first: sempre busca do servidor, cai no cache se offline.
  - Requisições POST (formulários CSRF, ações de serviço): ignoradas pelo SW.
  - Requisições para /admin/ e /media/: ignoradas pelo SW.

Comportamento do SW:
  - No evento INSTALL, faz cache de Bootstrap CSS, Bootstrap JS, Bootstrap Icons
    e Google Fonts Inter. Falhas individuais não impedem os demais recursos.
  - No evento ACTIVATE, limpa caches de versões anteriores (CACHE_NAME versionado).
  - Usa self.skipWaiting() e self.clients.claim() para ativação imediata.

Fallback de segurança:
  - Se o Service Worker falhar ou o navegador não suportar, a aplicação continua
    funcionando 100% normalmente no modelo web tradicional de requisições.
  - O script de registro no base.html verifica 'serviceWorker' in navigator antes
    de tentar registrar.

C. Rota do Service Worker (urls.py + views.py):
-------------------------------------------------
O SW precisa ser servido da RAIZ do site (/service-worker.js) para ter escopo
sobre toda a aplicação (/). Para isso:
  - URL  : /service-worker.js  (name='service_worker')
  - View : service_worker_view — lê o arquivo static e serve com:
      Content-Type: application/javascript
      Service-Worker-Allowed: /
      Cache-Control: no-cache
  - Não requer autenticação (o SW deve ser registrado na tela de login).

D. Integração no Template Base (base.html):
---------------------------------------------
Adicionado no <head> (após viewport):
  - {% load static %} no topo do template
  - <link rel="manifest" href="{% static 'maintenance/manifest.json' %}">
  - <meta name="theme-color" content="#1e1b4b">
  - <meta name="apple-mobile-web-app-capable" content="yes">
  - <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  - <meta name="apple-mobile-web-app-title" content="Manutenção">
  - <link rel="apple-touch-icon" href="{% static 'maintenance/pwa-icons/icon-192.png' %}">
  - <meta name="mobile-web-app-capable" content="yes">

Adicionado antes do </body> (após o script de dismiss de alertas):
  - Script de registro do Service Worker com verificação de suporte.

E. Como Instalar no Celular/Tablet:
-------------------------------------
  Android (Chrome):
    1. Acesse o sistema pelo navegador Chrome no celular.
    2. O navegador exibirá automaticamente um banner "Adicionar à tela de início".
    3. Toque em "Instalar" ou acesse o menu (⋮) > "Instalar app".
    4. O ícone do Painel de Manutenção aparecerá na tela inicial do dispositivo.

  iOS (Safari):
    1. Acesse o sistema pelo Safari no iPhone/iPad.
    2. Toque no botão de compartilhamento (quadrado com seta para cima).
    3. Selecione "Adicionar à Tela de Início".
    4. Confirme o nome e toque em "Adicionar".

F. Segurança e Integridade:
-----------------------------
- Nenhuma tabela do banco de dados foi criada, alterada ou excluída.
- Nenhum novo app Django foi criado.
- Nenhum modelo existente foi modificado.
- O Service Worker opera de forma progressiva — falhas são silenciosas.


25. EXPIRAÇÃO ABSOLUTA DE SESSÃO POR PERFIL DE USUÁRIO (2026-06-19)
--------------------------------------------------------------------------------
Implementação de controle de turno por expiração absoluta de sessão, evitando
o acúmulo de sessões ativas na troca de turnos no chão de fábrica.

Arquivos criados:
- maintenance/middleware.py  [NOVO]

Arquivos modificados:
- maintenance_project/settings.py
- Instrucoes.txt

NENHUMA MIGRAÇÃO necessária — apenas configuração de sessão e middleware.

A. Configuração de Sessão (settings.py):
-----------------------------------------
Variáveis adicionadas:
  SESSION_COOKIE_AGE = 14400
    → Tempo máximo de vida da sessão: 4 horas (14.400 segundos).
    → Após 4 horas desde o momento do LOGIN, o cookie expira automaticamente
      e o técnico é deslogado, independentemente de atividade.

  SESSION_EXPIRE_AT_BROWSER_CLOSE = True
    → A sessão também expira ao fechar o navegador.

  SESSION_SAVE_EVERY_REQUEST = False
    → CRÍTICO para expiração ABSOLUTA: o Django NÃO renova o timestamp
      da sessão a cada requisição. Se fosse True, o tempo de 4h seria
      de inatividade (idle timeout), não de tempo total logado.

B. Middleware de Exceção por Perfil (middleware.py):
----------------------------------------------------
Classe: SessionExpiryByProfileMiddleware

Posicionamento: APÓS django.contrib.auth.middleware.AuthenticationMiddleware
(garantindo que request.user já esteja disponível).

Lógica:
  1. Verifica se o usuário está autenticado.
  2. Na primeira requisição após o login, verifica se:
     - username == 'tv'  OU
     - pertence ao grupo 'Visualizador'
  3. Se SIM → força request.session.set_expiry(315360000) = ~10 ANOS
     (sessão perpétua para o painel da TV na fábrica).
  4. Se NÃO → não faz nada (o SESSION_COOKIE_AGE de 4h se aplica).
  5. Marca a sessão com flag '_session_expiry_checked' = True para
     NÃO repetir a consulta ao grupo a cada requisição subsequente.

Performance:
  - A consulta ao grupo ocorre UMA ÚNICA VEZ por sessão (na primeira request).
  - Todas as requests subsequentes verificam apenas a flag booleana na sessão.

C. Resumo de Comportamento por Perfil:
---------------------------------------
Perfil                        | Tempo de Sessão | Comportamento
------------------------------|-----------------|-----------------------------------
Técnico (TECNICO)             | 4 horas         | Deslogado ao fim do turno
Técnico Líder (TECNICO_LIDER) | 4 horas         | Deslogado ao fim do turno
Operador (OPERADOR)           | 4 horas         | Deslogado ao fim do turno
Superuser / Staff             | 4 horas         | Deslogado ao fim do turno
Visualizador (grupo)          | ~10 anos        | Sessão perpétua (painel TV)
Usuário 'tv' (username)       | ~10 anos        | Sessão perpétua (painel TV)

D. Segurança e Integridade:
-----------------------------
- Nenhuma tabela do banco de dados foi criada, alterada ou excluída.
- Nenhum novo app Django foi criado.
- Nenhum modelo existente foi modificado.
- O middleware é transparente e não altera nenhum fluxo de view existente.
- Se o middleware for removido, o sistema continua funcionando (SESSION_COOKIE_AGE
  se aplica a todos os perfis igualmente — incluindo o Visualizador/TV).



Boa pergunta! O banner automático de "Adicionar à tela de início" do Chrome tem critérios rigorosos que precisam ser atendidos simultaneamente. O motivo mais provável no seu caso:

🔒 O principal: HTTPS é obrigatório
O Chrome exige conexão HTTPS para exibir o prompt de instalação do PWA. Como você está rodando com python manage.py runserver 0.0.0.0:8000, o servidor Django de desenvolvimento serve apenas HTTP (sem SSL). Nesse cenário:

O Service Worker não registra (bloqueado pelo navegador em HTTP)
O manifesto é lido, mas o Chrome não oferece a instalação
A única exceção é localhost / 127.0.0.1 — ali o Chrome permite PWA em HTTP para fins de desenvolvimento. Mas acessando pelo IP da rede (ex: http://192.168.x.x:8000) no celular, é HTTP puro e o prompt nunca aparece.

✅ Como resolver (opções):
Produção com HTTPS (ideal): Ao publicar o sistema com Nginx/Apache + certificado SSL (Let's Encrypt), o prompt aparecerá automaticamente no celular.

Teste rápido no celular (ngrok): Use o ngrok para criar um túnel HTTPS temporário:

ngrok http 8000
Ele gera uma URL https://xxxx.ngrok.io que você acessa no celular — com HTTPS, o PWA funciona.

Instalação manual (funciona em HTTP): Mesmo sem o prompt automático, no Chrome mobile você pode ir no menu (⋮) → "Instalar app" ou "Adicionar à tela de início" — em alguns dispositivos isso funciona mesmo em HTTP.

Outros critérios do Chrome para o prompt automático:
✅ Ter manifest.json com name, icons, start_url, display — já temos
✅ Ter um Service Worker registrado — já temos (mas precisa de HTTPS)
⚠️ O usuário precisa ter interagido com o site por pelo menos 30 segundos
⚠️ O usuário não pode ter recusado a instalação anteriormente
Resumindo: o sistema PWA está configurado corretamente. O prompt aparecerá quando o acesso for por HTTPS (produção com certificado SSL ou via ngrok para testes).

10:21 AM
como tudo será rodado local na fabrica como posso resolver isso? não altere ou edite nada só me responda
10:30 AM
Para ambiente 100% local na fábrica (LAN, sem internet e sem domínio), existem duas abordagens práticas:

Opção 1: Certificado SSL autoassinado (Recomendada) 🏭
Você gera um certificado SSL diretamente no PC da fábrica e roda o Django com HTTPS local. Os passos seriam:

Instalar o pacote django-extensions + pyOpenSSL (ou Werkzeug):

pip install django-extensions pyOpenSSL
Adicionar django_extensions ao INSTALLED_APPS no settings.py

Rodar o servidor com HTTPS:

python manage.py runserver_plus 0.0.0.0:8000 --cert-file cert.pem
Ele gera automaticamente o certificado autoassinado na primeira vez.

No celular, ao acessar https://192.168.x.x:8000, o navegador vai avisar "Conexão não segura" — toque em "Avançado" → "Continuar mesmo assim". Após aceitar, o PWA funciona normalmente e o prompt de instalação aparece.

O certificado é "falso" (autoassinado), mas o Chrome aceita PWA com certificado autoassinado em rede local após o usuário aceitar o aviso.

Opção 2: Flag do Chrome no celular (mais simples, sem mudar nada no servidor)
No celular Android de cada técnico, ativar uma flag que permite PWA em HTTP:

Abra o Chrome no celular
Digite na barra de endereço:
chrome://flags/#unsafely-treat-insecure-origin-as-secure
No campo que aparecer, digite o endereço do servidor:
http://192.168.x.x:8000
Mude o dropdown para "Enabled"
Toque em "Relaunch" para reiniciar o Chrome
Pronto — aquele celular passa a tratar o IP da fábrica como se fosse HTTPS. O Service Worker registra e o prompt de instalação aparece. Nada muda no servidor.

⚠️ Desvantagem: precisa configurar em cada celular individualmente.

Minha sugestão para a fábrica:
Cenário	Melhor opção
Poucos celulares (3-5 técnicos)	Opção 2 — flag no Chrome, zero mudança no servidor
Muitos celulares ou rotatividade alta	Opção 1 — certificado autoassinado, configura uma vez no servidor
Qual cenário se encaixa melhor na fábrica? Se quiser, posso implementar a Opção 1 no projeto.


25. ADAPTAÇÃO DE REGRAS DE PROGRAMAÇÃO E CORREÇÃO DO BUG DE FINALIZAÇÃO (2026-06-22)
--------------------------------------------------------------------------------
Este item documenta a adaptação da pasta regras_programacao/ e a correção do bug 
de finalização de serviços pausados.

A. Adaptação de Regras e Constituição (regras_programacao/):
---------------------------------------------------------
- Prompt-padrão.txt: Atualizado para apontar corretamente para a pasta de regras
  deste projeto (regras_programacao/).
- SPEC_TEMPLATE.md: Adaptado para contemplar os perfis (Técnico, Técnico Líder, 
  Operador, Visualizador), módulos (maintenance, maintenance_project) e contextos 
  reais do painel.
- constitution.md: Refatorado para descrever os objetivos, padrões de código,
  regras de banco e regras de negócio do Painel de Manutenção Industrial 
  (concorrência, SQLite/MySQL, refreshes da TV, etc.).
- constitution.md (raiz): Sincronizado e atualizado com a nova versão estendida
  e unificada das regras de desenvolvimento do projeto.

B. Correção do Bug de Finalização de Serviços Pausados (views.py / templates):
----------------------------------------------------------------------------
- views.py: Modificadas as views finish_service e finish_allocation para, em caso
  de erro de validação (ex: observação em branco), redirecionar de volta para a
  tela de gerenciamento com parâmetros de consulta na URL indicando o modal a ser
  reaberto e seu respectivo ID (?open_modal=finish_tech&tech_id=... ou ?open_modal=finish_alloc&alloc_id=...).
- technician_management.html (JS):
  - Adicionadas checagens defensivas (if (!button) return;) no início de todos
    os event listeners dos modais (show.bs.modal) para evitar erros de TypeError 
    quando disparados programaticamente.
  - Substituído o script IIFE de auto-reabertura por um analisador de parâmetros
    da URL. Se a URL contiver o parâmetro open_modal, ele identifica e localiza 
    o botão de disparo correspondente no DOM e abre o modal enviando o botão como
    relatedTarget para o Bootstrap. No caso de serviços pausados, o script também
    abre automaticamente a seção colapsável do técnico correspondente para boa UX.
- Benefício: Resolve o problema de envios incorretos em POST para /management/ 
  e impede crashes de JS que travavam a usabilidade do painel após erros de digitação.


26. INDIVIDUALIZAÇÃO DO MODAL DE FINALIZAÇÃO DE SERVIÇOS PAUSADOS (2026-06-22)
--------------------------------------------------------------------------------
Este item documenta a individualização do modal de finalização de serviços pausados,
corrigindo de forma definitiva o problema de submissões incorretas para /management/.

Arquivos modificados:
- maintenance/templates/maintenance/technician_management.html
- Instrucoes.txt

A. Modificações Realizadas no Template (technician_management.html):
--------------------------------------------------------------------
1. Modais de Finalização Pausados Únicos: Mover o modal de finalização de serviços
   pausados (`finishAllocModal`) de uma definição externa genérica para dentro do loop 
   de alocações pausadas (`{% for p_alloc in paused %}`). Cada alocação possui agora seu próprio 
   modal com ID único `finishAllocModal-{{ p_alloc.id }}` e action estática apontando diretamente
   para a URL correta `{% url 'finish_allocation' p_alloc.id %}`.
2. Identificadores Únicos dos Inputs: Atualizados os IDs dos campos do modal (textarea e input de foto)
   para utilizarem o ID da alocação correspondente (`id_fa_observacao_conclusao-{{ p_alloc.id }}`),
   garantindo compatibilidade com o HTML5/DOM e prevenindo conflitos entre múltiplos modais.
3. Simplificação e Remoção de JS redundante: Excluída a lógica Javascript que alterava a action
   do formulário dinamicamente no evento `show.bs.modal`, visto que agora a action é gerada de
   forma estática e segura no backend via template engine.
4. Auto-Reabertura Avançada: Ajustada a lógica de auto-reabertura programática pós-erro de validação
   no backend para localizar e instanciar o modal correto com o ID único `finishAllocModal-${allocId}`.
5. Permissões Fortificadas (Segurança): Substituídas todas as validações de permissões frágeis
   baseadas no índice de grupos (`request.user.groups.all.0.name`) pelas variáveis de contexto robustas
   `user_can_manage` e `tech.id == technician_proprio_id`, resolvendo problemas de visualização
   para superusuários ou usuários com múltiplos grupos.